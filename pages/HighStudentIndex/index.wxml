<view class="head">
  <view class="head_title">高中生初选合格信息登记</view>
</view>
<view class="wrap">
  <view class="info_list">
    <view class="info_item first_info" bindtap="toHighstudentInfo">1.基本信息填报<text wx:if="{{stage1_status == 1}}">(已填报)</text></view>
    <view class="info_item" bindtap="toPrimarySuccessInfo">2.初选合格3日内填报<text wx:if="{{stage2_status==1 }}">(已填报)</text></view>
    <view class="info_item" bindtap="toJoinedInfo">3.参检后续填报<text wx:if="{{stage3_status == 1}}">(已填报)</text></view>
    <view class="info_item" bindtap="toGuessScoreInfo">4.高考估分填报<text wx:if="{{stage4_status == 1}}">(已填报)</text></view>
    <view class="info_item" bindtap="toPolityInfo">政治考核个人信息填报<text wx:if="{{stage5_status == 1}}">(已填报)</text></view>
    <view class="info_item" bindtap="toPolityCheckInfo">政治考核调查线索填报<text wx:if="{{stage6_status == 1}}">(已填报)</text></view>
  </view>
</view>