<!-- 头部 -->
<view class="head">
    <view class="head_title">初中生初选合格信息登记</view>
</view>
<!-- 中间内容 -->
<view class="wrap">
    <view class="info_list">
        <view class="info_item first_info" bindtap="toBaseInfo">1.基本信息填报<text wx:if="{{stage1_status == 1}}">（已填报）</text></view>
        <view class="info_item" bindtap="toQualified" >2.初选合格3日内填报 <text wx:if="{{stage2_status == 1}}">（已填报）</text></view>
        <view class="info_item" bindtap="toPolityInfo">政治考核个人信息填报 <text wx:if="{{stage3_status == 1}}">（已填报）</text></view>
        <view class="info_item" bindtap="toPolityCheckInfo">政治考核调查线索填报 <text wx:if="{{stage4_status == 1}}">（已填报）</text></view>
    </view>
</view>